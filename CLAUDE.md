# 쿠팡 파트너스 인스타 게시물 제작 지침 (휴대전화 채팅용)

사용자가 채팅으로 **상품 캡처 이미지들 + "링크 N번"** 을 보내면, 아래 순서를
반드시 지킨다. Claude가 캡처를 직접 분석·카피 작성하고, 렌더링은
`src/card_generator.py` 의 `render_promo_cards()` 를 사용한다 (API 키 불필요).

## 절대 규칙

1. **처음에는 이미지를 만들지 않는다.** 캡처 분석 → 타이틀 후보 → 인스타
   본문/해시태그를 먼저 보여주고 사용자의 선택·수정을 기다린다.
2. 이미지는 사용자가 "1번 이미지 만들어줘"처럼 요청할 때 **한 번에 1장씩만**
   만든다. 4장 동시 생성, 4분할, 콜라주, 모아보기 금지.
3. 모든 이미지에 필수 포함: **"광고" 표시**, **"프로필 링크에서 N번을
   검색하세요"**, **"쿠팡 파트너스 활동의 일환으로 일정액의 수수료를 제공받을
   수 있습니다."**, 상품명/핵심 정보, 대표 상품 이미지.
   (render_promo_cards 가 광고 표시·링크 문구·고지를 자동으로 그린다.)
4. 규격: 1080x1080 정사각형, 상품 크게, 문구는 핵심만, 과장·허위 금지
   (캡처에서 확인된 내용만).

## 이미지 4장의 역할

| 번호 | 종류 | 내용 |
|---|---|---|
| 1 | 메인 홍보 카드 | 선택된 메인 타이틀 + 서브 + 핵심 특징 1~2개 + 상품 사진 (circles 형) |
| 2 | 후기 만족 포인트 카드 | 후기 제목 + 후기 장점 3~4개 (rows 리스트형) |
| 3 | 제품 정보 카드 | 제품명/소재/용량/구성/기능 등 스펙 (rows 리스트형) |
| 4 | 추천 대상 카드 | 추천 제목 + 추천 대상 3~4개 (circles 또는 rows) |

## 타이틀 제안 형식 (캡처 받은 직후 출력)

```
[타이틀 후보]
- 메인 타이틀 1~3
- 서브 타이틀 1~2
- 후기 만족 포인트 제목
- 제품 정보 제목
- 추천 대상 제목
```

이어서 인스타 게시물 내용을 아래 형식으로 작성:

```
[인스타 제목] / [인스타 본문] (후킹 첫 문장, 핵심 3~4줄, 추천 포인트, 구매 유도)
프로필 링크에서 N번을 검색하세요
[광고 문구] 쿠팡 파트너스 활동의 일환으로 일정액의 수수료를 제공받을 수 있습니다.
[해시태그] 5~10개
```

마지막에 반드시: "원하는 타이틀을 골라주세요. 선택하시면 인스타 게시물 내용과
1번 이미지부터 한 장씩 제작할게요."

## 렌더링 방법 (참고)

캡처 원본은 `/root/.claude/uploads/<session>/` 에 저장된다. 상품 사진은 가장
깨끗한 캡처에서 PIL 로 잘라내 사용한다 (잘라낸 뒤 반드시 눈으로 확인).

```python
from src import card_generator as cg
cg.render_promo_cards([card_dict], out_dir, product_image=photo, link_no="101")
```

card_dict: kicker / title=[(텍스트,"red"|"purple"),...] / banner /
circles=[(줄1,줄2)x3] 또는 rows=[문장...] / tag(선택).
생성 후 이미지는 SendUserFile 로 1장씩 전달한다.

## 문구 원칙

- 짧고 강하게, 모바일 가독성 우선, 첫 문장에 후킹
- 캡처에 보이는 사실만 사용 (가격 이벤트는 변동 가능하니 단정 표현 금지)
- 카드 렌더 문구에는 이모지 금지 (폰트 미지원), 캡션에는 이모지 허용
