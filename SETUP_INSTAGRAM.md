# 인스타그램 토큰 / IG_USER_ID 발급 단계별 안내

자동 업로드에 필요한 `IG_USER_ID` 와 `IG_ACCESS_TOKEN` 을 얻는 과정입니다.
화면이 자주 바뀌므로 메뉴 이름이 조금 다를 수 있습니다.

> 핵심: 인스타 **비즈니스 계정** → **페이스북 페이지 연결** → **Meta 앱** →
> **토큰 발급** → (도우미 스크립트로) **장기 토큰 + IG_USER_ID 자동 추출**

---

## 1단계. 인스타그램을 비즈니스(또는 크리에이터) 계정으로 전환

1. 인스타그램 앱 → **설정 및 개인정보** → **계정 유형 및 도구** → **프로페셔널 계정으로 전환**
2. **비즈니스** 선택

## 2단계. 페이스북 페이지에 연결

1. 페이스북에서 **페이지**가 없다면 새로 하나 만듭니다 (이름은 자유).
2. 인스타그램 앱 → **설정** → **비즈니스** → **(페이지 연결)** 에서 그 페이지를 연결합니다.
   - 또는 페이스북 페이지 → **설정** → **연결된 계정** → Instagram 연결.

> ✅ "인스타 비즈니스 계정 ↔ 페이스북 페이지" 연결이 가장 중요합니다.
> 이게 안 되어 있으면 뒤 단계에서 IG_USER_ID 가 안 나옵니다.

## 3단계. Meta 개발자 앱 만들기

1. https://developers.facebook.com/ 접속 → 로그인 → **내 앱** → **앱 만들기**
2. 유형은 **비즈니스** 선택, 앱 이름 입력 후 생성
3. 만든 앱의 **설정 → 기본 설정** 에서 **앱 ID(App ID)** 와 **앱 시크릿(App Secret)** 을 메모해 둡니다.

## 4단계. Graph API Explorer에서 짧은 수명 토큰 받기

1. https://developers.facebook.com/tools/explorer/ 접속
2. 오른쪽 위 **Meta App** 에서 방금 만든 앱 선택
3. **User or Page** → **User Token** 선택
4. **Add a Permission** 에서 아래 권한을 모두 체크:
   - `instagram_basic`
   - `instagram_content_publish`
   - `pages_show_list`
   - `pages_read_engagement`
   - (필요 시) `business_management`
5. **Generate Access Token** 클릭 → 팝업에서 본인 계정/페이지 허용
6. 생성된 긴 문자열(`EAAG...`)이 **짧은 수명 토큰**입니다. 복사해 둡니다.

> ⏳ 이 토큰은 약 1~2시간 후 만료됩니다. 다음 단계를 바로 진행하세요.

## 5단계. 도우미 스크립트로 장기 토큰 + IG_USER_ID 자동 추출

가장 헷갈리는 마지막 단계(토큰 변환·ID 조회)를 스크립트가 대신 해줍니다.

```bash
python get_credentials.py \
    --app-id    <3단계의 앱 ID> \
    --app-secret <3단계의 앱 시크릿> \
    --token     <4단계의 짧은 수명 토큰>
```

성공하면 이렇게 출력됩니다:

```
📄 페이지: 내 카페 (id=10987...)
📸 인스타: @my_cafe

👉 .env 에 아래 값을 넣으세요:

IG_USER_ID=178414...
IG_ACCESS_TOKEN=EAAG...(장기 토큰)
```

출력된 두 줄을 `.env` 파일에 그대로 붙여넣으면 끝입니다.

> 💡 장기 사용자 토큰에서 나온 **페이지 토큰은 사실상 만료되지 않습니다.**
> 단, 비밀번호 변경·권한 회수 시에는 다시 발급해야 합니다.

## 6단계. 확인

```bash
python -m src.main --news --dry-run
```

오류 없이 이미지·캡션이 만들어지면 토큰 설정이 정상입니다.
이제 업로드(`--news`)나 웹 UI(`streamlit run app.py`)를 쓰면 됩니다.

---

## 자주 막히는 부분

| 증상 | 원인 / 해결 |
|------|-------------|
| 스크립트가 "연결된 인스타 계정 없음" | 1·2단계(비즈니스 전환·페이지 연결) 재확인 |
| `(#10) ... permission` 오류 | 4단계 권한 누락 → 권한 체크 후 토큰 재발급 |
| 업로드 시 `media ... not ready` | 이미지 URL이 만료(약 1시간)됨 → 생성 직후 바로 업로드 |
| 토큰이 자꾸 만료 | 짧은 수명 토큰을 그대로 썼을 때 → 반드시 5단계로 장기 토큰 변환 |

> 권한 검수(App Review): 본인 계정으로 테스트하는 단계에서는 보통 추가 검수 없이
> 동작합니다. 다른 사람 계정에 게시하려면 Meta의 앱 검수가 필요합니다.
