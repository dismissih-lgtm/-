# 카카오톡 '나에게 보내기' 설정 단계별 안내

매일 뉴스로 만든 **이미지 + 캡션을 내 카카오톡으로 받는** 기능 설정입니다.
페이스북·인스타 API가 전혀 필요 없고, 친구 승인이나 사업자 심사도 없습니다.
(받은 걸 보고 인스타에는 직접 올리면 됩니다.)

> 핵심: 카카오 개발자 앱 만들기 → 카카오 로그인 켜기 → talk_message 동의항목 켜기
> → 도우미 스크립트로 리프레시 토큰 발급 → `.env` 에 넣기

---

## 1단계. 카카오 개발자 앱 만들기

1. https://developers.kakao.com 접속 → 카카오 계정 로그인
2. 상단 **내 애플리케이션** → **애플리케이션 추가하기**
3. 앱 이름·사업자명(자유) 입력 후 저장
4. 만든 앱 → **앱 키** 메뉴에서 **REST API 키** 복사해 둡니다.

## 2단계. 카카오 로그인 켜기 + Redirect URI 등록

1. 왼쪽 메뉴 **제품 설정 → 카카오 로그인** → **활성화 설정 ON**
2. 같은 화면 아래 **Redirect URI 등록** → `https://localhost` 입력 후 저장

## 3단계. 메시지 전송 동의항목 켜기

1. **제품 설정 → 카카오 로그인 → 동의항목**
2. **카카오톡 메시지 전송(talk_message)** 항목을 찾아 **사용**으로 설정
   - 상태가 "필요 시 동의" 또는 "선택 동의" 면 됩니다.

## 4단계. 도우미 스크립트로 리프레시 토큰 발급

```bash
python get_kakao_token.py --rest-api-key <1단계의 REST API 키>
```

1. 출력된 **동의 주소**를 브라우저에서 열고 **동의하기** 클릭
2. `https://localhost/?code=XXXX...` 같은 **빈 페이지로 이동**합니다
   (브라우저에 "사이트에 연결할 수 없음" 떠도 정상 — 주소창의 주소가 중요)
3. 그 **주소 전체**를 복사해 스크립트에 붙여넣기
4. 출력된 두 줄을 `.env` 에 넣습니다:

```
KAKAO_REST_API_KEY=...
KAKAO_REFRESH_TOKEN=...
```

## 5단계. 테스트

```bash
python -m src.main --news --to kakao
```

내 카카오톡의 **'나와의 채팅'** 에 이미지 카드와 캡션이 오면 성공입니다! 🎉

---

## 자주 막히는 부분

| 증상 | 원인 / 해결 |
|------|-------------|
| `refresh_token 이 없음` | 3단계 talk_message 동의항목이 꺼져 있음 → 켜고 다시 발급 |
| `KOE006` / redirect 불일치 | 2단계 Redirect URI 와 스크립트의 `--redirect-uri` 가 달라서 → 둘 다 `https://localhost` 로 맞추기 |
| `KOE101` | REST API 키가 틀림 → 앱 키 메뉴에서 다시 확인 |
| 메시지가 안 옴 | 토큰을 만든 그 카카오 계정 본인에게만 전송됩니다(나와의 채팅 확인) |

> 리프레시 토큰은 약 2개월 유효하며, 사용할 때마다 자동 갱신됩니다.
> 오래 안 쓰면 만료될 수 있는데, 그땐 4단계를 다시 하면 됩니다.
